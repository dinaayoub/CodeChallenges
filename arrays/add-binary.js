/**
 * @param {string} a
 * @param {string} b
 * @return {string}
 */

var addBinary = function (a, b) {

  //edge case checking
  if (!a || a === '0') return b;
  if (!b || b === '0') return a;
  let aArray = a.split('');
  let bArray = b.split('');
  let result, n;
  if (a.length > b.length) {
    result = 
  }
  else n = b.length;

  for (let i = n - 1; i >= 0; i--) {
    if 
  }

};